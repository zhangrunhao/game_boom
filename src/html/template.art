<div class="outer">
  {{each}}
  <div class="row">
    {{each $value}}
    <div class="col">
      {{if !$value.isBlank}}
      {{$value.num}}
      {{/if}}
    </div>
    {{/each}}
  </div>
  {{/each}}
</div>